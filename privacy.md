# MindTrace 隐私政策

最后更新：2026-05-19

MindTrace（「本扩展」）由开发者提供，用于在浏览网页时记录个人灵感。**我们不会收集、出售或分享您的个人数据。**

## 数据存储位置

| 数据类型 | 存储方式 | 说明 |
|---------|---------|------|
| 灵感记录（原文、想法、页面标题与 URL） | `chrome.storage.local` | 仅保存在您的浏览器本地 |
| 语义向量（embedding） | 浏览器 **IndexedDB** | 仅保存在您的浏览器本地 |
| 关键词 | 与记录一并存在本地 | 由本地算法生成，不上传 |

所有数据**默认仅存在于您的设备**，扩展不提供云同步或远程备份功能。

## 本地语义模型

- 语义向量在**本地 Worker** 中计算（`lib/embedding.worker.js` + ONNX WASM）。
- 首次在「思维花园」或「思维宇宙」使用语义功能时，可能需您**同意可选权限**，以便从 Hugging Face / jsDelivr **下载**开源模型文件；下载后由浏览器缓存，**不会**将您的笔记文本发送到我们的服务器。
- 若您拒绝该权限，划词记录、灵感库浏览、关键词关联仍可使用；仅**语义相似度 / 图谱连线**等依赖模型的能力会受限。

## 我们不会做的事

- **不**追踪您的浏览历史或访问了哪些网站（除您主动保存的一条记录中的页面标题/URL 外，该信息仅写入您自己的本地存储）。
- **不**采集账号、密码、Cookie 或表单内容。
- **不**向第三方分析或广告服务发送数据。
- **不**在后台向所有网页自动注入脚本；仅在您**点击扩展图标**后，对**当前标签页**临时注入划词界面（`activeTab` + `chrome.scripting`）。

## 页面访问权限

本扩展申请的权限为：

- `storage` — 读写本地灵感记录  
- `activeTab` — 仅在您点击扩展时访问当前标签页  
- `scripting` — 在您点击扩展后向当前页注入划词 UI  

**不包含** `<all_urls>` 主机权限，**不包含** `tabs`、`webNavigation` 等可广泛读取标签信息的权限。

## 数据删除

- 在灵感库中删除单条记录，会同时清除对应的本地向量（如已实现）。
- 在 `chrome://extensions` 中移除本扩展，将清除扩展相关的 `chrome.storage` 与 IndexedDB 数据（以 Chrome 行为为准）。

## 联系我们

如对隐私有疑问，请通过扩展在 Chrome 网上应用店所列的支持渠道联系开发者。
